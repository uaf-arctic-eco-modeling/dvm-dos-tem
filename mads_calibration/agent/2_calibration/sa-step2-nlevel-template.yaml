# Step 2 N-level SA — micbnup + nmax -> AVLN (+ N-ratio diagnostic).
# Canonical also targets actual GPP; GPP field keys are outside this agent folder.
# First stage after seed. Analyze/apply: --phase nlevel
# See calibration_instructions.md

seed_path: {{SEED_PATH}}
observations: /work/calibration

N_samples: {{N_SAMPLES}}

work_dir: {{WORK_DIR}}

params: {{PARAMS_LIST}}
pftnums: {{PFTNUMS_LIST}}
percent_diffs: {{PERCENT_DIFFS}}
# p_bounds: [[0.01, 1.0], ...]

site: {{SITE}}
PXx: {{PXX}}
PXy: {{PXY}}

calib_mode: VEGC
sampling_method: lhc

target_names:
- AvailableNitrogenSum
- OrganicNitrogenSum

aux_outputs:
- INGPP y
- GPP y

cmtnum: {{CMTNUM}}

opt_run_setup: --pr-yrs 100 --eq-yrs 2000 --sp-yrs 0 --tr-yrs 0 --sc-yrs 0
