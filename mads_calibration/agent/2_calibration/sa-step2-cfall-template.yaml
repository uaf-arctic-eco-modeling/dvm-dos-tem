# Step 2 Cfall SA — copy into logs/ and replace {{PLACEHOLDERS}}.
# Canonical: Cfall -> VEGC after Krb is fixed (calibration.rst).
# Prerequisites: applied nlevel_pass AND krb_pass.
# Analyze: --phase cfall  |  Apply: --phase cfall
# See calibration_instructions.md

seed_path: {{SEED_PATH}}
observations: /work/calibration

N_samples: {{N_SAMPLES}}

work_dir: {{WORK_DIR}}

# cfall(0/1/2) per active PFT — propose_bounds.py --family cfall
params: {{PARAMS_LIST}}
pftnums: {{PFTNUMS_LIST}}
percent_diffs: {{PERCENT_DIFFS}}
# p_bounds: [[0.0, 0.2], ...]

site: {{SITE}}
PXx: {{PXX}}
PXy: {{PXY}}

calib_mode: VEGC
sampling_method: lhc

target_names:
- VegCarbon
- NPPAll

aux_outputs:
- INGPP y
- GPP y

cmtnum: {{CMTNUM}}

opt_run_setup: --pr-yrs 100 --eq-yrs 2000 --sp-yrs 0 --tr-yrs 0 --sc-yrs 0
