{
  "general": {
    "run_name": "multi site test case",
    "config_dir": "config/",
    "runmode": "multi"   // "single" or "multi"
  },

  "data_directories": {
    "run_cohort_list": "DATA/test_06_cohorts/runchtlist.nc",
    "output_data_dir": "DATA/test_06_cohorts/output/",
    "region_data_dir": "DATA/test_06_cohorts/dataregion/",
    "grid_data_dir":   "DATA/test_06_cohorts/datagrid/",
    "cohort_data_dir": "DATA/test_06_cohorts/datacht/"
  },

  "stage_settings": {
    "run_stage": "eq",          // Must be "eq","sp","tr","sc", or "all"
    "restart_mode": "restart",
    "restart_file": "DATA/test_06_cohorts/output/restart-eq.nc"
  },

  "model_settings": {
    "dynamic_climate": 0,  // 1 or -1 or default (0: upon to stage)
    "varied_co2": 0,       // 1 or -1  or default (0: upon to stage)
    "dynamic_lai": 1,               // from veg. C in model (1), or otherwise from input (0) 
    "fire_severity_as_input": 0,    // using fire severity as input (1), or otherwise derived from fire size/fire season/
    "output_starting_year": -9999  // '-9999' for starting model running after 'eq'
  },

  // Not sure if these are appropriately checked in mult mode from this file
  // previously the model set them internally it using a regional control file
  "output_switches": {
    "daily_output": 0,
    "monthly_output": 0,
    "yearly_output": 1,
    "summarized_output": 1,
    "soil_climate_output": 0
  }
}
